<h1>Poll created!</h1>

<p>Send the first link below to your desired pollees. Use the second link to access the poll admin view.</p>

<ul>
  <li> Poll Link: <a id="poll-url">{{ poll_url }}</a></li>
  <li> Admin View (current page???): <a id="admin-url">{{ dashboard_url }}</a></li>
</ul>

<h2 id="poll-name">{{ pollName }}</h2>

{{#each questions}}
<div class=""> {{@key}}
  <br>
  <button class="poll-question" type="button" name="button">{{this}}</button>
</div>
{{/each}}

<script type="text/javascript" src="/dashboard.js"></script>
